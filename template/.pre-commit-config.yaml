---
# this repository uses the pre-commit framework
# documentation can be found at https://pre-commit.com/
#
# perform once after cloning the repository:
#  pre-commit install
#
# if you want to trigger pre-commit manually:
#  pre-commit run
#  pre-commit run --all-files
#

default_install_hook_types:
  - pre-commit
  - commit-msg
  - post-commit
  - pre-push

default_stages:
  - pre-commit
  - pre-push

repos:
  # -----------------------------------------------------------------------
  # stage: pre-commit and pre-push
  # -----------------------------------------------------------------------
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-merge-conflict
        args: [--assume-in-merge]
      - id: check-json
      - id: check-toml
      - id: check-yaml
      - id: end-of-file-fixer
      - id: trailing-whitespace
  # https://pycqa.github.io/isort/docs/configuration/pre-commit.html
  - repo: https://github.com/pycqa/isort
    rev: 6.0.0
    hooks:
      - id: isort
  # https://pypi.org/project/autopep8/
  - repo: https://github.com/hhatto/autopep8
    rev: v2.3.2
    hooks:
      - id: autopep8
  # https://github.com/PyCQA/flake8
  - repo: https://github.com/PyCQA/flake8
    rev: 7.1.2
    hooks:
      - id: flake8
  # https://pylint.readthedocs.io/en/stable/index.html
  - repo: local
    hooks:
      - id: pylint
        name: scan for broken code
        entry: uv run pylint
        language: system
        types:
          - python
        require_serial: true
        args:
          # special configuration for commit validation:
          #  - ignore all non-breaking issues
          #  - require all breaking issues to be fixed
          - "--score=n"
          - "--disable=C,W,R"
          - "--fail-under=10"
  # -----------------------------------------------------------------------
  # stage: commit-msg
  # -----------------------------------------------------------------------
  - repo: https://github.com/commitizen-tools/commitizen
    rev: v4.2.2
    hooks:
      - id: commitizen
  # -----------------------------------------------------------------------
  # stage: post-commit
  # -----------------------------------------------------------------------
  # !! post-commit can't prevent the commit from happening !!
  #
  # At this point we can only complain that the commit wasn't signed
  # properly and provide a warning that something isn't right and remind
  # the user to go back and fix this specific commit.
  #
  # (We rely on the 'pre-push' stage to prevent pushing of unsigned commits.)
  - repo: local
    hooks:
      - id: commit-signed
        name: Verify commit is signed
        stages:
            - post-commit
        always_run: true
        language: system
        entry: git
        args: [ "verify-commit", "HEAD" ]
  # -----------------------------------------------------------------------
  # stage: pre-push
  # -----------------------------------------------------------------------
  - repo: local
    hooks:
      - id: signed-commits
        name: Verify signature
        language: script
        entry: scripts/verify_commits-are-signed
        args: [ "$PRE_COMMIT_FROM_REF", "$PRE_COMMIT_TO_REF" ]
        stages:
            - pre-push
  - repo: https://github.com/commitizen-tools/commitizen
    rev: v4.2.2
    hooks:
      - id: commitizen-branch
        stages:
          - pre-push
